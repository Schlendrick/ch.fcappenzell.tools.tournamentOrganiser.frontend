import { DndDirective } from './dnd.direcitves';

describe('DndDirective', () => {
    it('should create an instance', () => {
        const directive = new DndDirective();
        expect(directive).toBeTruthy();
    });
});